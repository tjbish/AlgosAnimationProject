Build & Run
-----------

Requirements:
- Node.js and npm

To build the project (bundles JS into `dist/bundle.js`):

```bash
npm run build
```

Serve locally (recommended) and open the app in your browser:

```bash
npm run start
```

You can also open `index.html` directly after `npm run build`, but using a local server avoids browser restrictions.
